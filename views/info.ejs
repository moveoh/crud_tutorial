<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Read Page</title>
</head>

<body>
    이 곳은 Read 기능을 구현할 페이지입니다.
</body>
<table>
    <tr>
        <th style="width: 100px;">이름</th>
        <th>아이디</th>
        <th style="width: 200px;">메일주소</th>
        <th>가입일</th>
    </tr>
    <%userInfo.forEach(function(user){%>
        <tr>
            <td style="width: 100px;"><%=user.name%></td>
            <td><%=user.userid%></td>
            <td style="width: 200px;"><%=user.email%></td>
            <td style="font-size:.4em;"><%=user.date%></td>
            <td><a href='/delete/<%=user.id%>'>삭제하기</a></td>
        </tr>
        <%});%>
</table>

<a href="/">메인으로 가기</a>

</html>
